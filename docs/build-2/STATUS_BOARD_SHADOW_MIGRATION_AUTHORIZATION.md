# GATE Build 2 — Status Board Shadow Migration Authorization

Status: CONSUMED / PHASE 3A SHADOW ACTIVE  
Authorization class: hidden, read-only, non-operational comparison

## Authorized scope consumed

Phase 3A now:

- consumes the active in-memory records list through canonical normalization;
- calculates the Build 2 Status Board summary in parallel with Build 1;
- compares arrived, expected, active buses, last arrival, dorm state, and timer behavior;
- retains aggregate comparison outcomes in memory only;
- exposes read-only diagnostics through a hidden runtime bridge;
- validates route-specific foreign-tab invalidation and authoritative refetch;
- defines six responsive posture, accessibility, and fullscreen contracts.

## Prohibited scope remains in force

The shadow package may not:

- replace or alter the visible Build 1 Status Board;
- change the default route;
- write operational records;
- register the staged service worker in Build 1;
- expose Build 2 as a visible operational route;
- retire a Build 1 controller, renderer, calculation, stylesheet, or middleware asset;
- treat a parity mismatch as permission to change Build 1 operational truth without a documented decision;
- advance directly to Processing or another write-capable route.

## Automated evidence completed

```text
PASS — same in-memory authoritative input records
PASS — active Week Group canonical context
PASS — arrived and projected total comparison
PASS — active bus identity comparison
PASS — derived last-arrival comparison
PASS — empty/open/closed dorm count and membership comparison
PASS — timer display, warning, critical, and overtime comparison
PASS — foreign-tab invalidation triggers authoritative refetch
PASS — all six responsive posture contracts
PASS — accessibility and fullscreen automated contracts
PASS — immediate rollback by removing the single shadow bridge asset
PASS — no visible route, record write, evidence persistence, or service worker
```

The automated suite also preserves the historical `911 / 857` Status Board fixture and explicitly reports Build 1 `last_airport` configuration versus Build 2 derived last-arrival truth as a blocking mismatch when they differ.

## Evidence still required

```text
PENDING — sustained live shadow samples across an operational receiving window
PENDING — live AF, SF, female, NAT, local-arrival, load, and indicator evidence where present
PENDING — disposition of every blocking live mismatch
PENDING — manual stale/offline read-only presentation review
PENDING — manual six-posture visual captures
PENDING — keyboard, focus, screen-reader, contrast, and reflow review
PENDING — fullscreen/command-display entry, exit, and focus-restoration review
PENDING — production deployment and rollback prerequisites
```

## Activation decision after evidence review

Shadow CI closure does not authorize production activation. A later Status Board activation package must identify:

- the exact Build 1 owner being replaced;
- the Build 2 route host and feature flag;
- retained live shadow evidence;
- production deployment prerequisites;
- manual accessibility and command-display results;
- mismatch disposition;
- activation sequence;
- rollback sequence;
- monitoring period;
- legacy retirement criteria.

## Current decision

```text
AUTHORIZED — continue hidden Phase 3A evidence collection
NOT AUTHORIZED — Phase 3B controlled test surface
NOT AUTHORIZED — visible Build 2 Status Board
NOT AUTHORIZED — Build 2 production writes
NOT AUTHORIZED — Build 1 Status Board retirement
```

## Route order

Status Board remains first. Processing, Airport, Input, Archives/Reports, and Squadron Board remain blocked until the Status Board route receives its separate activation decision and the route-order gate advances.
